# ===== Model loading =====
MODEL_DIR=models

# Default selection if caller omits fields
DEFAULT_TAG=B                # A | B | AFF
DEFAULT_FRAMEWORK=lgbm       # lgbm | lstm | bilstm

# Classification threshold
THRESHOLD_UP=0.50

# ===== Uvicorn (local dev) =====
HOST=0.0.0.0
PORT=8000

# ===== CORS (optional, if you wire it up) =====
# Comma-separated list with no trailing slashes
# CORS_ALLOW_ORIGINS=http://localhost:5173,https://your-frontend.example.com
